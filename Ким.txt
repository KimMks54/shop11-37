import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: MyHomePage(),
    );
  }
}

class MyHomePage extends StatefulWidget {
  @override
  _MyHomePageState createState() => _MyHomePageState();
}
class _MyHomePageState extends State<MyHomePage> {
  int _selectedIndex = 0;
  
  List<Widget> _widgetOptions = <Widget>[
    GridView.count(
      crossAxisCount: 3,
      padding: EdgeInsets.all(8.0),
      children: [
        Padding(
          padding: const EdgeInsets.all(8.0),
          child: Container(
            decoration: BoxDecoration(
              color: Color.fromARGB(255, 133, 107, 132),
              borderRadius: BorderRadius.circular(10.0), // Закругление углов
            ),
            child: Center(child: Text('Товар 1')),
          ),
        ),
        Padding(
          padding: const EdgeInsets.all(8.0),
          child: Container(

            decoration: BoxDecoration(
              
              color: Color.fromRGBO(255, 133, 107, 132),
              borderRadius: BorderRadius.circular(10.0),
            ),
            child: Center(child: Text('Товар 2')),
            
          ),
        ),
      Padding(
          padding: const EdgeInsets.all(8.0),
          child: Container(
            decoration: BoxDecoration(
              color: Color.fromARGB(255, 133, 107, 132),
              borderRadius: BorderRadius.circular(10.0),
            ),
            child: Center(child: Text('Товар 3')),
          ),
      ),
       Padding(
          padding: const EdgeInsets.all(8.0),
          child: Container(
            decoration: BoxDecoration(
              color: Color.fromARGB(255, 133, 107, 132),
              borderRadius: BorderRadius.circular(10.0),
            ),
            child: Center(child: Text('Товар 4')),
          ),
      ),
             Padding(
          padding: const EdgeInsets.all(8.0),
          child: Container(
            decoration: BoxDecoration(
              color: Color.fromARGB(255, 133, 107, 132),
              borderRadius: BorderRadius.circular(10.0),
            ),
            child: Center(child: Text('Товар 5')),
          ),
      ),
             Padding(
          padding: const EdgeInsets.all(8.0),
          child: Container(
            decoration: BoxDecoration(
              color: Color.fromARGB(255, 133, 107, 132),
              borderRadius: BorderRadius.circular(10.0),
            ),
            child: Center(child: Text('Товар 6')),
          ),
      ),
        // Добавьте закругление для остальных контейнеров
      ],
    ),
    Text(
      'Избранное',
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
    ),
    Text(
      'Корзина',
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
    ),
  ];

  void _onItemTapped(int index) {
    setState(() {
      _selectedIndex = index;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Магазин'),
      ),
      body: Column(
        children: [
          Padding(
            padding: const EdgeInsets.all(8.0),
          ), 
          Expanded(
            child: Center(
              child: _widgetOptions.elementAt(_selectedIndex),
            ),
          ),
        ],
      ),
      bottomNavigationBar: BottomNavigationBar(
        items: const <BottomNavigationBarItem>[
          BottomNavigationBarItem(
            icon: Icon(Icons.home),
            label: 'Главная',
          ),
          BottomNavigationBarItem(
            icon: Icon(Icons.favorite),
            label: 'Избранное',
          ),
          BottomNavigationBarItem(
            icon: Icon(Icons.shopping_cart),
            label: 'Корзина',
          ),
        ],
        
        currentIndex: _selectedIndex,
        selectedItemColor: Colors.blue,
        onTap: _onItemTapped,
      ),
    );
  }
}